import { defineConfig } from 'vite';

export default defineConfig({
  // If your GitHub Pages is served from sightser.github.io (user/organization page),
  // the base should be the root path '/'.
  base: '/',
});